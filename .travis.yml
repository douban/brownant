language: python
python:
  - "2.7"
  - "3.3"
  - "pypy"
install:
  - "pip install . --use-mirrors"
  - "pip install pytest pytest-cov pytest-pep8 mock coveralls --use-mirrors"
script: "py.test"
after_success:
  coveralls
branches:
  only:
    - master
    - develop
